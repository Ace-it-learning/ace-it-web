runtime: nodejs20
service: default

env_variables:
  NODE_ENV: "production"
  # Add other environment variables here if needed
  # VERTEX_ENABLED: "true" # Optional, GenerativeAIService auto-detects CLOUD_RUN_SERVICE too

automatic_scaling:
  min_instances: 0
  max_instances: 5
